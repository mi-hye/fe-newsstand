"use strict";

const GRID = Object.freeze({
	cellCount: 24,
	firstPageIdx: 0,
	lastPageIdx: 3,
});

const VISIBILITY = Object.freeze({
	hidden: "hidden",
	visible: "visible",
});

const ROLLING = Object.freeze({
	newsCount: 5,
	fourSec: 4000,
	fiveSec: 5000,
	firstNewsIdx: 0,
	lastNewsIdx: 4,
});
